---
apiVersion: v1
kind: Service
metadata:
  name: '{{ .Values.movieService.serviceName }}'
spec:
  type: '{{ .Values.movieService.serviceType }}'
  ports:
    - port: 8888
      targetPort: 8888
      nodePort: 8888
  selector:
    app: movie-service
